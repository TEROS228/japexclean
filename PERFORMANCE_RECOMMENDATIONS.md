# Рекомендации по производительности

## Текущее состояние (до 50 пользователей)
✅ **Сделано:**
- Кеш в памяти 1 час
- Максимум 3 одновременных Puppeteer процесса
- Дедупликация запросов

## Если будет 50-100 пользователей одновременно

### 1. Redis кеш (высокий приоритет)
```bash
npm install ioredis
```

**Преимущества:**
- Кеш сохраняется при перезапуске сервера
- Можно увеличить до 24 часов
- Несколько серверов могут использовать один кеш

### 2. Предзагрузка популярных товаров
- Раз в час автоматически парсить топ-100 товаров
- Большинство пользователей получат данные из кеша

### 3. Фоновые задачи (Bull/BullMQ)
```bash
npm install bull
```

**Что это даст:**
- Парсинг происходит в фоне
- Пользователь получает "Loading..." и ждет результат
- Сервер не падает под нагрузкой

### 4. Отдельный сервер для Puppeteer
- Основной сервер (Next.js) отдельно
- Puppeteer сервер отдельно
- Общаются через API или очереди

**Стоимость:** ~$5-10/месяц за второй VPS

## Если будет 500+ пользователей

### 1. Cloudflare Workers + R2
- Кеш на CDN
- Мгновенная отдача по всему миру

### 2. Database кеш (PostgreSQL/MongoDB)
- Хранить распарсенные данные
- Обновлять раз в сутки через cron

### 3. Отказаться от Puppeteer
- Использовать только API (Rakuten, Yahoo)
- Для недоступных данных - показывать "View on Rakuten"

## Мониторинг

Добавить метрики:
```typescript
// pages/api/metrics.ts
let totalRequests = 0;
let cacheHits = 0;
let cacheMisses = 0;
let puppeteerTimeout = 0;

export default function handler(req, res) {
  return res.json({
    totalRequests,
    cacheHitRate: (cacheHits / totalRequests * 100).toFixed(2),
    activePuppeteer: activePuppeteerCount,
    uptime: process.uptime()
  });
}
```

## Текущие лимиты
- ✅ До 50 пользователей: всё ОК
- ⚠️ 50-100 пользователей: добавить Redis
- 🔴 100+ пользователей: нужны все оптимизации выше
